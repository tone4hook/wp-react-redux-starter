import React from "react";

const Example = props => {
	return (
		<div>
			<h1 className="text-center">Example View</h1>
			<p className="lead text-center">
				An Example View that displays when navigating from a{" "}
				<strong>custom link</strong> added to the Main Navigation Menu.
			</p>
		</div>
	);
};

export default Example;
